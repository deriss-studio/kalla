# Production points at managed Postgres in an EU region.
# Development and test use PGlite in process — no connection string needed.
DATABASE_URL=

# Model access goes through src/lib/models.ts and nowhere else.
# A provider that cannot report its processing region is not eligible.
MISTRAL_API_KEY=
MODEL_DEFAULT_REGION=eu-west-1
